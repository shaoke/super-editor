{
  "name": "super-editor",
  "version": "0.1.0",
  "description": "A markdown editor, can sync up with Evernote",
  "private": true,
  "license": "Apache-2.0 ",
  "author": "Shaoke Xu",
  "repository": "shaoke/super-editor",
  "devDependencies": {
    "babel-core": "^5.8.25",
    "del": "^2.0.2",
    "gulp": "^3.9.0",
    "gulp-autoprefixer": "^3.0.2",
    "gulp-cache": "^0.3.0",
    "gulp-concat": "^2.4.1",
    "gulp-connect": "^2.2.0",
    "gulp-css-inline-images": "^0.1.1",
    "gulp-jshint": "^1.6.3",
    "gulp-load-plugins": "^0.10.0",
    "gulp-sass": "2.0.*",
    "gulp-uglify": "^1.0.1",
    "gulp-util": "^3.0.4",
    "run-sequence": "^1.0.2"
  },
  "engines": {
    "node": ">=0.12.0"
  },
  "scripts": {
    "test": "gulp && git status | grep 'working directory clean' >/dev/null || (echo 'Please commit all changes generated by building'; exit 1)"
  },
  "babel": {
    "only": "gulpfile.babel.js"
  }
}
